# V6.60.1 — CI / Dependabot hotfix

- Corrige o guard de acessibilidade para normalizar o módulo `typescript` e não depender de `ScriptTarget`/`ScriptKind` existirem no mesmo formato em upgrades futuros.
- Corrige o contrato do Monte seu Kit para validar caminhos realmente renderizados pela Home (Header/MerlinMobileDock/InspirationShowcase), evitando falso negativo por busca textual rígida.
- CI passa a executar explicitamente os contratos Merlin adicionados após a V6.54.
- Dependabot deixa de abrir upgrades **major** automáticos de `typescript`, `zod` e `@types/node`; esses saltos exigem migração dedicada.
- Nenhuma alteração de schema, banco ou package-lock.
