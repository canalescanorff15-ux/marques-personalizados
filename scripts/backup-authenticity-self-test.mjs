process.env.BACKUP_SIGNING_SECRET='backup-self-test-primary-secret-2026-abcdefghijklmnopqrstuvwxyz';
process.env.BACKUP_SIGNING_PREVIOUS_SECRET='';
process.env.BACKUP_SIGNING_KEY_ID='self-test-primary';
const crypto=await import('node:crypto');
const {createBackupSignature,verifyBackupSignature}=await import('../lib/backup-signature.ts');
const {verifyBackupText,BACKUP_REQUIRED_SECTIONS}=await import('../lib/backup-restore.ts');
function hash(value){return crypto.createHash('sha256').update(value).digest('hex');}
function make(){const content=Object.fromEntries(BACKUP_REQUIRED_SECTIONS.map(k=>[k,k==='settings'?{id:1}:[]]));const counts=Object.fromEntries(BACKUP_REQUIRED_SECTIONS.filter(k=>k!=='settings').map(k=>[k,0]));const created_at='2026-09-09T18:00:00.000Z';const content_sha256=hash(JSON.stringify(content));const base={version:7,backup_format:'marques-catalog-v7',schema_version:20,created_at,content_sha256,counts};const signature=createBackupSignature(base);return{...base,content,backup:{version:7,backup_format:'marques-catalog-v7',schema_version:20,created_at,manifest:{algorithm:'sha256',content_sha256,counts,signature},...content}};}
const original=make();
const verified=verifyBackupText(JSON.stringify(original.backup),{requireAuthenticity:true});if(verified.authenticity.status!=='verified'||verified.authenticity.key_source!=='current')throw new Error('assinatura atual não validada');
// Ataque que SHA simples não impediria: altera o conteúdo e recalcula o SHA, mas não possui a chave HMAC.
const tampered=structuredClone(original.backup);tampered.settings={id:1,site_name:'ALTERADO'};const tamperedContent=Object.fromEntries(BACKUP_REQUIRED_SECTIONS.map(k=>[k,tampered[k]]));tampered.manifest.content_sha256=hash(JSON.stringify(tamperedContent));let rejected=false;try{verifyBackupText(JSON.stringify(tampered),{requireAuthenticity:true});}catch(error){rejected=error instanceof Error&&error.message==='BACKUP_SIGNATURE_INVALID';}if(!rejected)throw new Error('adulteração com SHA recalculado não foi rejeitada');
// Rotação: a chave anterior continua verificável temporariamente.
const oldSignature=original.backup.manifest.signature;process.env.BACKUP_SIGNING_SECRET='backup-self-test-rotated-secret-2026-ABCDEFGHIJKLMNOPQRSTUVWXYZ';process.env.BACKUP_SIGNING_PREVIOUS_SECRET='backup-self-test-primary-secret-2026-abcdefghijklmnopqrstuvwxyz';const rotated=verifyBackupSignature({version:7,backup_format:'marques-catalog-v7',schema_version:20,created_at:original.created_at,content_sha256:original.content_sha256,counts:original.counts},oldSignature,{requireAuthenticity:true});if(rotated.key_source!=='previous')throw new Error('rotação com chave anterior falhou');
const unsigned=structuredClone(original.backup);delete unsigned.manifest.signature;const inspected=verifyBackupText(JSON.stringify(unsigned),{requireAuthenticity:false});if(inspected.authenticity.status!=='unsigned')throw new Error('backup legado não identificado como unsigned');let unsignedRejected=false;try{verifyBackupText(JSON.stringify(unsigned),{requireAuthenticity:true});}catch(error){unsignedRejected=error instanceof Error&&error.message==='BACKUP_SIGNATURE_REQUIRED';}if(!unsignedRejected)throw new Error('backup unsigned não foi bloqueado em modo obrigatório');
console.log('Backup Authenticity Self-test: OK (HMAC atual, SHA recalculado rejeitado, rotação e legado unsigned).');
