import fs from 'node:fs';const errors=[];const r=f=>fs.readFileSync(f,'utf8');const need=(f,t,l)=>{if(!r(f).includes(t))errors.push(`${l}: ${t}`)};for(const f of ['scripts/media-recovery-drill.mjs','lib/media-recovery-drill.ts','.github/workflows/media-recovery-drill.yml','sql/schema.sql'])if(!fs.existsSync(f))errors.push(`ausente ${f}`);for(const t of ['media_recovery_drill_receipts','media_recovery_drill_receipts_append_only','idx_media_recovery_drill_receipts_succeeded'])need('sql/schema.sql',t,'schema');for(const t of ['MEDIA_RECOVERY_DRILL_CONFIRM_ISOLATED','DRILL_S3_ENDPOINT','source-sha256','DeleteObjectsCommand','MEDIA_RECOVERY_DRILL_OK'])need('scripts/media-recovery-drill.mjs',t,'runner');for(const t of ['schedule:','MEDIA_RECOVERY_DRILL_CONFIRM_ISOLATED','media:recovery-drill'])need('.github/workflows/media-recovery-drill.yml',t,'workflow');for(const t of ['media_recovery_drill','getMediaRecoveryDrillFreshness'])need('app/api/health/route.ts',t,'health');need('.github/workflows/ci.yml','npm run check:media-recovery-drill','CI');const p=JSON.parse(r('package.json'));if(errors.length){console.error(`Media Recovery Drill Check: ${errors.length}`);for(const e of errors)console.error('- '+e);process.exit(1)}console.log('Media Recovery Drill Check: OK');
