import crypto from 'node:crypto';
function digest(content){return crypto.createHash('sha256').update(JSON.stringify(content)).digest('hex');}
const sections=['products','archived_products','categories','inquiries','settings','testimonials','faqs','audit','inquiry_activity','social_content_plans','marketing_campaigns','site_events_daily'];
const content={products:[],archived_products:[],categories:[],inquiries:[],settings:{id:1},testimonials:[],faqs:[],audit:[],inquiry_activity:[],social_content_plans:[],marketing_campaigns:[],site_events_daily:[]};
const hash=digest(content);const backup={version:7,backup_format:'marques-catalog-v7',schema_version:11,created_at:new Date().toISOString(),manifest:{algorithm:'sha256',content_sha256:hash,counts:Object.fromEntries(sections.filter(k=>k!=='settings').map(k=>[k,0]))},...content};
const calculated=digest(Object.fromEntries(sections.map(k=>[k,backup[k]])));if(calculated!==hash){console.error('Self-test restore: hash canônico divergente');process.exit(1);}backup.products.push({id:'x'});const tampered=digest(Object.fromEntries(sections.map(k=>[k,backup[k]])));if(tampered===hash){console.error('Self-test restore: adulteração não detectada');process.exit(1);}const planA=crypto.createHash('sha256').update(JSON.stringify({contract:'marques-restore-v1',backup_sha256:hash,schema_version:11,counts:{products:0}})).digest('hex');const planB=crypto.createHash('sha256').update(JSON.stringify({contract:'marques-restore-v1',backup_sha256:hash,schema_version:11,counts:{products:1}})).digest('hex');if(planA===planB){console.error('Self-test restore: plano não está vinculado às contagens');process.exit(1);}console.log('Restore Integrity Self-test: OK (hash, adulteração e vínculo do plano).');
