import fs from 'node:fs';const errors=[];const read=f=>fs.readFileSync(f,'utf8');const need=(f,t,l)=>{if(!read(f).includes(t))errors.push(`${l}: ${t}`)};need('lib/db.ts','requireCriticalAuditIntent','Helper strict');need('lib/db.ts',"throw new Error('AUDIT_STORAGE_UNAVAILABLE')",'Fail closed');need('lib/db.ts',"RETURNING id,integrity_hash",'Persistência comprovada');const critical=['backup/route.ts','settings/route.ts','sessions/route.ts','security/route.ts','media/route.ts','inquiries/[id]/route.ts','categories/[id]/route.ts','campaigns/[id]/route.ts','products/[id]/route.ts','privacy/route.ts','restore/route.ts','operations/route.ts'];for(const rel of critical){const f=`app/api/admin/${rel}`;need(f,'requireCriticalAuditIntent',`Rota crítica ${rel}`);}need('.github/workflows/ci.yml','npm run check:critical-audit','CI');const pkg=JSON.parse(read('package.json'));if(errors.length){console.error(`Critical Audit Check: ${errors.length}`);for(const e of errors)console.error('- '+e);process.exit(1)}console.log('Critical Audit Check: OK');
