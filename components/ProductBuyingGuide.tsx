import { CheckCircle2, ClipboardCheck, Sparkles, WalletCards } from 'lucide-react';
import type { Product } from '@/lib/db';
import { productBuyingGuide } from '@/lib/product-guide';
export default function ProductBuyingGuide({product}:{product:Product}){const guide=productBuyingGuide(product);return <section className="product-buying-guide" aria-labelledby="product-guide-title"><div className="product-buying-guide-head"><div><div className="eyebrow">ANTES DE PEDIR</div><h2 id="product-guide-title">O que ajuda a fechar<br/><em>este orçamento mais rápido.</em></h2></div><p>Um briefing melhor reduz retrabalho e deixa o valor final mais previsível.</p></div><div className="product-buying-guide-grid"><article><Sparkles size={20}/><strong>Ideal para</strong>{guide.ideal.map(item=><span key={item}><CheckCircle2 size={14}/>{item}</span>)}</article><article><ClipboardCheck size={20}/><strong>O que você define</strong>{guide.define.map(item=><span key={item}><CheckCircle2 size={14}/>{item}</span>)}</article><article><WalletCards size={20}/><strong>O que altera o valor</strong>{guide.valueDrivers.map(item=><span key={item}><CheckCircle2 size={14}/>{item}</span>)}</article><article><ClipboardCheck size={20}/><strong>Prepare antes de enviar</strong>{guide.preparation.map(item=><span key={item}><CheckCircle2 size={14}/>{item}</span>)}</article></div></section>;}
